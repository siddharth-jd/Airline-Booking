PORT=4003

DB_HOST=localhost
DB_PORT=5432
DB_NAME=bookings_db
DB_USER=bookings_svc
DB_PASSWORD=change_me

JWT_SECRET=change_me

FLIGHTS_SERVICE_URL=http://localhost:4002

# Set to true ONLY in local development — mounts the deliberately racy demo endpoint.
ENABLE_UNSAFE_DEMO=false

KAFKA_BROKERS=localhost:9092
KAFKA_TOPIC=booking-events
RELAY_POLL_INTERVAL_MS=1000
RELAY_BATCH_SIZE=100
